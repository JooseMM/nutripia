import { CloseMenuOnclickDirective } from './close-menu-onclick.directive';

describe('CloseMenuOnclickDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseMenuOnclickDirective();
    expect(directive).toBeTruthy();
  });
});
